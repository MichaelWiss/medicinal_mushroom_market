// Next.js client instrumentation hook (Cell 7.2).
//
// Auto-loaded by Next 15 in the browser bundle — runs once at app
// boot. Delegates to `sentry.client.config.ts` so the actual SDK
// initialisation lives in a single, conventional location.

import './sentry.client.config';
