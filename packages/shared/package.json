{
  "name": "@repo/shared",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "devDependencies": {
    "@repo/db": "workspace:*",
    "typescript": "*",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
